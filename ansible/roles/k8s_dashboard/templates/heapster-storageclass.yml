apiVersion: storage.k8s.io/v1beta1
kind: StorageClass
metadata:
  name: heapster-storage
  namespace: kube-system
  labels:
    app: heapster
    heapster: heapster-storage
provisioner: kubernetes-incubator/nfs
